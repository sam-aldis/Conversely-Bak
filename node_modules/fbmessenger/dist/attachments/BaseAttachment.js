"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
class BaseAttachment {
  constructor(_ref) {
    var type = _ref.type,
        url = _ref.url,
        is_reusable = _ref.is_reusable,
        attachment_id = _ref.attachment_id;

    this.type = type;
    this.url = url;
    this.is_reusable = is_reusable;
    this.attachment_id = attachment_id;

    var payload = {};

    if (this.url) {
      payload.url = url;
    }

    if (this.is_reusable) {
      payload.is_reusable = is_reusable;
    }

    if (this.attachment_id) {
      payload.attachment_id = attachment_id;
    }

    return {
      attachment: {
        type: this.type,
        payload
      }
    };
  }
}

exports.default = BaseAttachment;